export interface Maquina {
    id?: string; // ID del documento en Firebase (opcional)
    name: string;
    cores: string;
    ram: string;
    disk: string;
    os: string;
    status: string;
    
  }